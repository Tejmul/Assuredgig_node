services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: assuredgig
    ports:
      - "5432:5432"
    volumes:
      - assuredgig_pg:/var/lib/postgresql/data

volumes:
  assuredgig_pg:
